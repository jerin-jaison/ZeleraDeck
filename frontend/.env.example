# ZeleraDeck — Frontend Environment Variables
# Copy this to .env.local and fill in. Never commit .env.local to git.

# Your deployed Render backend URL — NO trailing slash
VITE_API_URL=https://your-backend.onrender.com/api/

# Admin panel password (must match ADMIN_SECRET_KEY in backend .env)
VITE_ADMIN_PASSWORD=zeleraadmin2025
